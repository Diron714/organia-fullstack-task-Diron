#!/bin/sh
# Maven Wrapper startup (Apache Maven Wrapper 3.2.0)
set -euf
MAVEN_PROJECTBASEDIR="${MAVEN_PROJECTBASEDIR:-$(cd "$(dirname "$0")" && pwd)}"
WRAPPER_JAR="$MAVEN_PROJECTBASEDIR/.mvn/wrapper/maven-wrapper.jar"
if [ ! -f "$WRAPPER_JAR" ]; then
  echo "maven-wrapper.jar missing at $WRAPPER_JAR" >&2
  exit 1
fi
if [ -z "${JAVA_HOME-}" ]; then
  JAVACMD="${JAVACMD:-java}"
else
  JAVACMD="${JAVACMD:-"$JAVA_HOME/bin/java"}"
fi
exec "$JAVACMD" ${MAVEN_OPTS:-} \
  -classpath "$WRAPPER_JAR" \
  "-Dmaven.multiModuleProjectDirectory=$MAVEN_PROJECTBASEDIR" \
  org.apache.maven.wrapper.MavenWrapperMain "$@"
